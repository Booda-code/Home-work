void main() {
  //  Explain what happens if you try to use a variable before it is declared in Dart.

  String name;
  print(name);

  // Call error because, the non-nullable local variable 'name' must be assigned before it can be used
}
